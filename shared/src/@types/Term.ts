type Term = {
    id: string,
    expression?: string,
    expressionImage?: string,
    translation?: string,
    translationImage?: string,
    setId: string,
    isNew?: boolean
};
export default Term;